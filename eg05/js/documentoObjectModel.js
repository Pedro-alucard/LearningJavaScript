
// SELECT A INDIVIDUAL ELEMENT
// select a indidual element by your id attribute

var el = document.getElementById();

// Uses a CSS selector, and returns
// the first matching element.
// return just one element 

document.querySelector();


// SELECT MULTIPLES ELEMENTS
// selects all elements that have a specific value for their class attribute

document.getElementsByClassName();

// Selects all elements that have the
// specified tag name <p> or <span> <div>..

document.getElementsByTagName();

// Uses a CSS selector to select all
// matching elements.

document.querySelectorAll();

// TRAVERSING BETWEEN
// ELEMENT NODES

// Selects the parent of the current
// element node (which will return
// just one element).

el.parentNode;

// Selects the previous or next
// sibling from the DOM tree. 

el.nextSibling;

// Select the first or last child of the
// current element. 
// el.firstChild / el.lastChild

